# This file is managed by Ansible — do not edit manually
network:
  ethernets:
    ens18:
      addresses:
        - {{ hostvars[inventory_hostname]['ansible_host'] }}/24
      routes:
        - to: default
          via: {{ gateway }}
      nameservers:
        addresses:
          - {{ dns1 }}
          - {{ dns2 }}
  version: 2
